schema_version: mmprism.experiment.v1
name: pose-smoke
task: pose_reconstruction
paths:
  data_root: ${MMPRISM_DATA_ROOT:-data}
  artifact_root: ${MMPRISM_ARTIFACT_ROOT:-outputs}
  cache_root: ${MMPRISM_CACHE_ROOT:-.cache/mmprism}
runtime:
  seed: 42
  accelerator: auto
  devices: auto
  precision: 32-true
  deterministic: true
